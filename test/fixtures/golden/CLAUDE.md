<!-- Generated by Architect Companion. Edit .architect-companion/ instead. -->

# Claude Instructions

Project: sample-project
Profile: Modular Monolith (modular-monolith 0.1.0)
Stack: typescript

## Principles

- Modules own behavior behind public APIs: Treat each module as the owner of its business behavior, data access, and invariants. Other modules should depend on the module's public API instead of reaching into its internal files.

## Policies

### Module boundaries

Severity: error

Intent: Modules may import another module only when declared in allowed_dependencies and through that module's public API.

Cross-module imports must be declared in allowed_dependencies and target the public_api path declared for the dependency module. Imports from another module's internal files, or from undeclared module dependencies, are boundary violations unless the dependency is redesigned or a documented exception is introduced.

## Modules

- billing: src/modules/billing
  Public API: src/modules/billing/index.ts
  Allowed dependencies: identity
- identity: src/modules/identity
  Public API: src/modules/identity/index.ts
  Allowed dependencies: none

## Workflows

### Change a module boundary

1. Identify the modules affected by the proposed boundary change.
2. Decide whether the change belongs in a public API or inside one module.
3. Update the public API before changing consumers.
4. Remove cross-module internal imports introduced during the change.
5. Run the architecture boundary check once rendering and checks are available.

## Review Heuristics

### Architecture review

- Does the change introduce a new dependency between modules?
- Does every cross-module call use the target module's public API?
- Is shared behavior placed in a stable module instead of duplicated through internal imports?
- Would a future module extraction be harder after this change?

## Examples

### Module access

Good:

```ts
import { findCustomer } from "../identity";

const customer = await findCustomer(customerId);
```

Bad:

```ts
import { loadCustomerRow } from "../identity/internal/customer-repository";

const customer = await loadCustomerRow(customerId);
```
