# TypeScript Profile

This profile contains TypeScript-specific guidance and implementation metadata
for TypeScript architecture checks.

It can provide dependency-cruiser implementations for policies contributed by
other selected profiles, such as CLI layer boundaries or modular monolith
module boundaries.
